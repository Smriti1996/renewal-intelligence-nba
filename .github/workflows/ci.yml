name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    env:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: .

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      # 🔹 NEW STEP: generate data so member_features.parquet exists
      - name: Generate sample data
        run: |
          PYTHONPATH=. python -m src.data_gen.run_data_gen

      - name: Run tests
        run: |
          pytest -q

      - name: Build backend image
        run: |
          docker build -f Dockerfile.backend -t renewal-intel-backend:ci .

      - name: Build UI image
        run: |
          docker build -f Dockerfile.ui -t renewal-intel-ui:ci .
